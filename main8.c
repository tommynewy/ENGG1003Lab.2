#include <stdio.h>
/* Write a C program which reads a 5x5 array from a
file and finds the maximum value in the array. The
file stores float data in csv format. Each line
contains 5 floats, separated by commas. There
are 5 lines in the file. There are no commas at the
end of lines.
You may assume that the file does not contain
errors.*/

void max(float data);

int main () {
    float data[5][5]
    int row, col;
}

void max(float data) {
    
}